import React from 'react';

function LoginButton() {
    return (
        <button style={{ position: 'relative', zIndex: 1000 }} className="btn btn-primary text-white">
            Login
        </button>
    );
}

export default LoginButton;
